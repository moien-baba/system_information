# Live CPU and Memory Monitor

A small terminal dashboard that shows live CPU and memory usage as bar plots using `psutil` and `rich`.

## Install

```bash
pip install -r requirements.txt
```

## Run

```bash
python main.py
```

You can change the refresh rate:

```bash
python main.py --interval 0.5
```
